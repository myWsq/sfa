# Corruption Cases

Use this folder for malformed archives and expected failure assertions.

Target cases:

- bad magic / unsupported version
- invalid header CRC
- invalid manifest hash
- invalid frame hash
- strong trailer mismatch
