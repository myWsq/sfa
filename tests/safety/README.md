# Path Safety Cases

Use this folder for restore safety checks.

Target cases:

- absolute paths
- path traversal (`..`)
- empty path segments
- embedded NUL bytes
- pre-existing symlink escape attempts
