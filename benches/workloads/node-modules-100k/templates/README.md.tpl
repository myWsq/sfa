# {{PACKAGE_NAME}}

`{{PACKAGE_NAME}}` is part of the deterministic benchmark corpus used to evaluate SFA on a deeply nested `node_modules` tree.

## Package Intent

- package depth: `{{PACKAGE_DEPTH}}`
- package index: `{{PACKAGE_INDEX}}`
- child dependency count: `{{DEPENDENCY_COUNT}}`

## Notes

This package intentionally contains the same kinds of tiny text assets that are common in JavaScript dependency trees: entry modules, type declarations, package metadata, small config JSON files, and local CLI shims.

The dependency list for this package is:

{{DEPENDENCY_LIST}}
